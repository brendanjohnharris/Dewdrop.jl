# M2 validation scenarios (Brunel 2000, Vogels-Abbott 2005)

Research for M2 (reproduce statistically + classical CairoMakie figures). Raw structured result below.

```json
{
  "research": [
    {
      "scenario": "Brunel (2000) \"Dynamics of Sparsely Connected Networks of Excitatory and Inhibitory Spiking Neurons\" (J. Comput. Neurosci. 8:183-208). A sparse random recurrent network of N_E excitatory + N_I inhibitory leaky integrate-and-fire neurons with delta-current (instantaneous-PSP) synapses, fixed transmission delay, and an external Poisson drive replacing C_ext sparse inputs. Sweeping the inhibition/excitation ratio g and the external drive ratio η = ν_ext/ν_thr produces a rich phase diagram with four canonical dynamical regimes (SR, AI, SI-fast, SI-slow). This is THE reference benchmark for validating SNN simulators (NEST, Brian2, PyNN all ship it).",
      "neuron_model": "Leaky integrate-and-fire (LIF), current-based, identical for E and I cells. Subthreshold: τ_m dV/dt = -(V - V_L) + R·I(t), implemented in Brunel relative to rest so V_L = 0 mV. Parameters: τ_m = 20 ms (membrane time constant); firing threshold V_θ = θ = 20 mV; reset potential V_r = 10 mV; resting/leak potential V_L = V_rest = 0 mV; absolute refractory period τ_rp (t_ref) = 2.0 ms. The membrane equation is linear, so the exact exponential propagator applies (decay = exp(-dt/τ_m)). Voltage is dimensionless-mV measured from rest; effective R is folded into the PSP jump J (see synapse). Dewdrop mapping: LIF(; τ=20e-3, EL=0.0, Vθ=20e-3, Vr=10e-3, R=..., tref=2e-3) with the PSP amplitude carried by the synaptic weight rather than R.",
      "synapse_model": "Delta-current (CUBA, instantaneous) synapses: each presynaptic spike causes an immediate step jump in the POSTsynaptic membrane potential of amplitude J (excitatory) or -g·J (inhibitory), delivered after a fixed transmission delay D = 1.5 ms. PSP amplitude J = J_E = 0.1 mV (E); inhibitory PSP = J_I = -g·J. There is NO synaptic time constant in the strict Brunel model (synapses are Dirac-delta currents, i.e. instantaneous voltage kicks). Standard simulator implementation (Brian2/NEST/PyNN) uses on-spike voltage increments V += J / V += -g·J after delay D; the only synaptic state is the per-connection delay line. Dewdrop mapping: emulate delta synapses with CurrentSynapse (CUBA exponential) using a very small τ_syn (τ_syn ≪ dt is the delta limit; or implement a true on-spike voltage increment), and a per-synapse DelayBuffer set to D = 1.5 ms (= 15 steps at dt = 0.1 ms). Many implementations also model the external drive as the same delta-current with weight J_E.",
      "network": "N_E = 10000 excitatory, N_I = 2500 inhibitory (γ = N_I/N_E = 0.25), total N = 12500. Sparse random (fixed-probability, \"FixedProb\") connectivity with ε = 0.1, giving fixed in-degrees C_E = ε·N_E = 1000 recurrent excitatory inputs and C_I = ε·N_I = 250 recurrent inhibitory inputs per neuron (C = C_E + C_I = 1250). Each neuron also receives C_ext = C_E = 1000 external excitatory connections from independent Poisson sources. Weights: excitatory J_E = J = 0.1 mV; inhibitory J_I = -g·J (g is the inhibition/excitation ratio, the primary control parameter; balance occurs near g = γ^{-1}·... i.e. g≈4 where g·C_I ≈ C_E). External weight = J_E = 0.1 mV. Delay D = 1.5 ms on all connections.",
      "regimes_or_states": [
        "SR (synchronous regular): excitation-dominated, neurons fire almost periodically and nearly in synchrony; coordinate g = 3, η = ν_ext/ν_thr = 2 (Brunel Fig 8A). Low g (g < 4) with η > 1.",
        "AI (asynchronous irregular): stationary global rate, Poisson-like irregular single-cell firing, no global oscillation; coordinate g = 5, η = 2 (Brunel Fig 8B). Strong inhibition g ≳ 4 with moderate-to-high drive η ≳ 1.",
        "SI-fast (synchronous irregular, fast oscillation): global rate oscillates at a FAST frequency (~150-200 Hz, set by the synaptic delay D), individual neurons fire irregularly and sparsely at rates far below the global frequency; coordinate g = 6, η = 4 (Brunel Fig 8C). Strong inhibition + high drive.",
        "SI-slow (synchronous irregular, slow oscillation): global rate oscillates at a SLOW frequency (near the membrane time constant scale, tens of Hz / sub-firing-rate), irregular sparse single-cell firing; coordinate g = 4.5, η = 0.9 (Brunel Fig 8D). Near balance g≈4-4.5 with low drive η < 1."
      ],
      "canonical_figures": [
        "Per-regime composite panel (Brunel Fig 8 a-d style): for each of SR (g=3,η=2), AI (g=5,η=2), SI-fast (g=6,η=4), SI-slow (g=4.5,η=0.9) plot (top) a spike raster of ~50 neurons over a short window and (bottom) the population firing-rate histogram (instantaneous population rate, bin ~0.1 ms, smoothed) over the same window. Brian2 panel windows/scales: AI t=500-600ms rate 0-... ; SI-slow t=1000-1200ms; SI-fast t=1000-1200ms; SR t=1000-1200ms.",
        "Phase diagram in the (g, η = ν_ext/ν_thr) plane (Brunel Fig 8 / Fig 2): partition g∈[0,8], η∈[0,4] into the four regions SR, AI, SI-fast, SI-slow with the analytic instability boundary lines (mean-field/linear-stability) overlaid; mark the four representative points.",
        "Population-rate power spectrum / autocorrelation per regime: flat (AI), sharp peak at ~150-200 Hz (SI-fast), peak at low frequency (SI-slow), peak at single-cell rate (SR) — used to extract oscillation frequency.",
        "ISI distribution and CV_ISI per regime (regular near-delta for SR; exponential, CV≈1 for AI/SI).",
        "Single-neuron membrane potential V(t) trace per regime showing regular sawtooth (SR) vs noisy fluctuating sub-threshold drive (AI/SI)."
      ],
      "validation_statistics": [
        "ν_thr (threshold external rate) = θ / (J_E · C_E · τ_m) = 20 mV / (0.1 mV · 1000 · 20 ms) = 20/(0.1·1000·0.020) = 10 spikes/s = 10 Hz; ν_ext = η · ν_thr.",
        "SR (g=3, η=2): mean single-cell rate high (tens of Hz, near-periodic), CV_ISI ≈ 0 (≲0.2, highly regular); strong global oscillation at ~ the single-cell firing rate; large pairwise spike correlations (synchrony). Population rate shows sharp regular periodic peaks.",
        "AI (g=5, η=2): low/moderate mean rate (order ~tens of Hz; commonly ~20-40 Hz in this drive range), CV_ISI ≈ 1 (0.8-1.0, Poisson-like irregular); NO global oscillation (flat population-rate spectrum); near-zero pairwise correlations (asynchronous), population rate ~stationary with small fluctuations ~1/sqrt(N).",
        "SI-fast (g=6, η=4): individual firing irregular (CV_ISI ≈ 0.8-1.0) and SPARSE (single-cell rate ≪ global oscillation frequency); strong global oscillation at ~150-200 Hz (frequency ≈ 1/(2D)-ish, controlled by delay D=1.5 ms); clear narrow peak in population-rate power spectrum near 180-200 Hz.",
        "SI-slow (g=4.5, η=0.9): irregular sparse single-cell firing (CV_ISI ≈ 1), low single-cell rate; global oscillation at a SLOW frequency set by τ_m (order tens of Hz, well below the SI-fast peak and below firing-rate-driven SR); distinct low-frequency peak in population spectrum.",
        "Test assertions: (1) ν_thr == 10 Hz exactly. (2) CV_ISI(AI) ∈ [0.8,1.1] and oscillation power flat. (3) CV_ISI(SR) < 0.3 with dominant spectral peak at mean firing rate. (4) SI-fast dominant population-rate spectral peak ∈ ~[140,220] Hz. (5) SI-slow dominant peak << SI-fast peak (low tens of Hz). (6) AI pairwise spike-count correlation ≈ 0 (|r| ≲ 0.01); SR/SI correlations clearly > 0."
      ],
      "dewdrop_mapping": "Neurons: one LIF model instance shared by both populations — LIF(; τ=20e-3, EL=0.0, Vθ=20e-3, Vr=10e-3, R, tref=2e-3); statevars (:V,:refrac); the exact exponential propagator already used by Dewdrop's fixed-step engine is exact for Brunel's linear LIF. Populations: an E Population of 10000 and an I Population of 2500 (or one population of 12500 partitioned), built on the same LIF. Synapses: delta-current PSPs approximated by CurrentSynapse (CUBA exponential) in the delta limit (τ_syn ≪ dt) OR a direct on-spike V-increment; weights J_E=+0.1mV (E recurrent + external), J_I=-g·J (I recurrent). Connectivity: SparseCSR built from a FixedProb generator with ε=0.1 producing C_E=1000 / C_I=250 fixed-in-degree edges (E->all weight J, I->all weight -gJ). Delays: per-synapse DelayBuffer with maxdelay = round(D/dt) = round(1.5ms/0.1ms) = 15 steps (uniform delay D=1.5ms on every edge). External drive: counter-based Poisson generator (to come) emitting C_ext=1000 independent Poisson spike trains per neuron at rate ν_ext=η·ν_thr, each with weight J_E and delay D — efficiently collapsed to one effective Poisson input of rate C_ext·ν_ext per neuron (or a Normal/diffusion-approx current μ=C_ext·J·ν_ext·τ, σ²=C_ext·J²·ν_ext·τ for the mean-field check). Engine: fixed-step exponential-Euler at dt=0.1 ms; threshold V≥Vθ, reset to Vr, hold for tref=2ms; spikes deposited into DelayBuffer and collected after D. RNG: Dewdrop RNG/counter-based stream for both connectivity generation and Poisson drive. Run for ~1-2 s, discard transient, then compute rate / CV_ISI / population-rate spectrum / pairwise correlation as the validation statistics above.",
      "sources": [
        "Brunel, N. (2000). Dynamics of Sparsely Connected Networks of Excitatory and Inhibitory Spiking Neurons. J. Comput. Neurosci. 8(3):183-208. https://link.springer.com/article/10.1023/A:1008925309027 (PDF: https://www.mimuw.edu.pl/~miekisz/brunel.pdf)",
        "Brian2 documentation — Example: Brunel_2000 (reproduces Fig 8; gives N_E=10000, gamma=0.25, N_I=2500, epsilon=0.1, C_E=1000, C_ext=C_E, tau=20ms, theta=20mV, V_r=10mV, tau_rp=2ms, J=0.1mV, D=1.5ms, nu_thr=theta/(J*C_E*tau), dt=0.1ms, method='exact'; panel coords A:g=3,η=2 SR; B:g=6,η=4; C:g=5,η=2; D:g=4.5,η=0.9). https://brian2.readthedocs.io/en/stable/examples/frompapers.Brunel_2000.html",
        "Gerstner et al., Neuronal Dynamics — Exercise 10: Network of LIF neurons (Brunel). Defaults V_rest=0, V_reset=10mV, V_th=20mV, tau_m=20ms, t_ref=2ms, w0=0.1mV, g, epsilon=0.1, delay=1.5ms, Poisson input N=1000. https://neuronaldynamics-exercises.readthedocs.io/en/latest/exercises/brunel-network.html",
        "NEST / PyNN ship the Brunel network as a standard benchmark (brunel_delta_nest.py); same parameter set. OpenSourceBrain Brunel2000 project: https://github.com/OpenSourceBrain/Brunel2000",
        "Brunel, N. & Wang, X-J. (2003). What Determines the Frequency of Fast Network Oscillations With Irregular Neural Discharges? J. Neurophysiol. — fast SI oscillations ~150-200 Hz controlled by synaptic delay/time constant. https://www.cns.nyu.edu/wanglab/publications/pdf/brunel2003.pdf",
        "Fabrizio Musacchio (2024), Brunel network blog walkthrough (regime tour). https://www.fabriziomusacchio.com/blog/2024-07-21-brunel_network/",
        "Uncertainpy Brunel NEST example. https://uncertainpy.readthedocs.io/en/latest/examples/brunel.html"
      ]
    },
    {
      "scenario": "Vogels & Abbott (2005) balanced random network of leaky integrate-and-fire neurons --- the canonical \"VAbenchmark\" reproduced across simulators (Brian2, NEST, PyNN, ANNarchy, GeNN). A sparsely (2%) connected E/I network of 4000 LIF neurons that, after a brief external kick, settles into a SELF-SUSTAINED ASYNCHRONOUS IRREGULAR (AI) state: low (few-Hz) mean firing rate, near-Poisson irregularity (CV_ISI near 1), large subthreshold voltage fluctuations, and no global oscillation/synchrony. Two synapse variants exist: COBA (conductance-based, Benchmark 1) and CUBA (current-based, Benchmark 2). Definitive parameter source is Brette et al. (2007), Appendix 2, which transcribes the VA 2005 model exactly. NOTE: PyNN's VAbenchmarks.py uses a rescaled COBA conductance pair (4 nS / 51 nS) and CUBA (0.27 nS / 4.5 nS); the original VA/Brette COBA quanta are 6 nS / 67 nS.",
      "neuron_model": "Leaky integrate-and-fire (single compartment, passive). Membrane equation: C_m dV/dt = -g_L (V - E_L) + synaptic terms. Specific values (Brette et al. 2007 App. 2, = VA 2005): specific capacitance C = 1 µF/cm^2; leak conductance density g_L = 5e-5 S/cm^2; leak reversal E_L = V_rest = -60 mV (COBA) / -49 mV (CUBA, depolarized ~10 mV to ignite activity). Cell area 20,000 µm^2 gives absolute C_m = 0.2 nF (200 pF), absolute g_L = 10 nS, hence tau_m = C_m/g_L = 20 ms; input resistance 100 MOhm. Firing threshold V_th = -50 mV; reset V_reset = -60 mV; absolute refractory period t_ref = 5 ms. Equivalently in simulator units: tau_m = 20 ms, cm = 0.2 nF, V_rest = -60 mV (COBA)/-49 mV (CUBA), V_thresh = -50 mV, V_reset = -60 mV, tau_refrac = 5 ms. (Same neuron for E and I populations.)",
      "synapse_model": "Exponential synapses: instantaneous rise, single-exponential decay (no rise time, no NMDA). COBA (conductance-based, Benchmark 1): on a presynaptic spike the synaptic conductance g_ji is incremented by a fixed quantum and decays exponentially; synaptic current = -g(t)(V_i - E_rev). Excitatory reversal E_ex = 0 mV, inhibitory reversal E_in = -80 mV; decay tau_exc = 5 ms, tau_inh = 10 ms; peak (quantal) conductances g_exc = 6 nS, g_inh = 67 nS (original VA/Brette). CUBA (current-based, Benchmark 2): current = -g(t)(Vbar - E_rev) with fixed Vbar = -60 mV, i.e. fixed-amplitude exponentially-decaying current pulses; same tau_exc = 5 ms, tau_inh = 10 ms; conductance quanta g_exc = 0.27 nS, g_inh = 4.5 nS (-> EPSC ~ 0.27 nS x 60 mV = 16.2 pA, IPSC ~ 4.5 nS x 20 mV = -90 pA; NEST iaf_psc_exp uses 16.2 pA / -139.5 pA after its kernel normalization). Synaptic transmission delay = 0.1 ms in NEST/Brette reproductions (PyNN VAbenchmarks uses 0.2 ms; PCSIM scalability runs used 1 ms). A single global delay is applied to all synapses.",
      "network": "N = 4000 LIF neurons: 3200 excitatory (80%) + 800 inhibitory (20%). Connectivity is random with fixed connection probability epsilon = 0.02 (2%), drawn independently for every ordered pair (FixedProb / Bernoulli), giving mean in-degree ~80 (about 64 E + 16 I inputs per neuron); autapses/multapses per simulator convention. Weights: COBA g_exc = 6 nS, g_inh = 67 nS (PyNN variant 4 nS / 51 nS); CUBA g_exc = 0.27 nS, g_inh = 4.5 nS. The inhibition-dominance ratio is built into the balance: |g_inh|/g_exc ~ 67/6 ~ 11.2 (COBA) and 4.5/0.27 ~ 16.7 (CUBA), with the I/E weight ratio combining with the 80/20 population split to give net inhibition-dominated balance that stabilizes the low-rate AI state. There is NO continuous external (Poisson) drive in the self-sustaining benchmark --- the network sustains itself once ignited; ignition uses a transient external kick (see regimes/initial stimulation).",
      "regimes_or_states": [
        "Self-sustained ASYNCHRONOUS IRREGULAR (AI) state: the target/operating regime. After the 50 ms ignition the network maintains activity indefinitely with no external drive; all cells fire irregularly with strong subthreshold V fluctuations, low pairwise correlation, and no population oscillation. This is the only intended steady state of the benchmark.",
        "COBA (Benchmark 1): conductance-based realization of the AI state (E_ex=0, E_in=-80 mV; g 6/67 nS).",
        "CUBA (Benchmark 2): current-based realization (Vbar=-60 mV, E_L depolarized to -49 mV; g 0.27/4.5 nS); produces a qualitatively identical AI state but is analytically simpler and used for scaling/precision benchmarks.",
        "Quiescent / die-out state: if the ignition kick is too weak or inhibition too strong, activity decays to silence --- the complementary fixed point. The benchmark is tuned so the AI state is self-sustaining rather than decaying.",
        "(Related variants in Brette App. 2, not the core scenario: COBA-HH Benchmark 3 with Hodgkin-Huxley neurons; CUBADV/voltage-jump Benchmark 4 with +0.25 mV / -2.25 mV voltage-jump synapses.)"
      ],
      "canonical_figures": [
        "Spike raster: spike times (y = neuron index) for a few hundred ms to 1 s, typically a random subset (e.g. 50-500 neurons, or E cells); shows sparse, irregular, asynchronous firing with no vertical (synchronous) bands.",
        "Population firing-rate histogram (PSTH): network-wide instantaneous rate vs time in small bins; should be roughly flat/stationary at a few Hz with only small fluctuations (no oscillatory peaks), confirming asynchrony.",
        "Single-cell membrane-potential trace V(t): subthreshold trajectory of one neuron showing large, noise-driven fluctuations and occasional threshold crossings/spikes (Brette Fig. 12 left panels); optionally the membrane-potential distribution (histogram of V) sitting well below threshold with broad spread.",
        "ISI histogram / CV_ISI distribution: interspike-interval distribution (near-exponential, Poisson-like) and the distribution of per-neuron CV_ISI across the population, peaked near 1 to demonstrate irregularity.",
        "(Optional/classical) Phase or balance diagram: mean rate / sustainability vs the g_inh:g_exc (inhibition-dominance) ratio or external-input strength, delineating the AI region from quiescent and synchronous-regular regions."
      ],
      "validation_statistics": [
        "Mean firing rate: low, single-digit Hz --- the self-sustained AI state for the standard VA/Brette parameters sits at roughly 4-10 Hz population-mean (commonly cited ~5 Hz; exact value depends on COBA vs CUBA and the 6/67 vs 4/51 nS variant). Assertion: 2 Hz < mean_rate < 15 Hz, stationary in time.",
        "CV_ISI near 1 (Poisson-like irregularity): population-mean CV_ISI ~ 0.8-1.2. Assertion: 0.7 < mean(CV_ISI) < 1.3, confirming irregular (not regular/bursting) firing.",
        "Asynchrony (low spike correlation): mean pairwise spike-count correlation small, |r| ~ 0 (typically < 0.1); population rate has no dominant oscillation frequency (flat power spectrum, no peak) --- distinguishes AI from synchronous-irregular states.",
        "Self-sustaining: activity persists for the full simulation (>= 1 s, often tested to 10 s) after the external stimulus is removed at t = 50 ms; total spike count is non-zero and roughly stationary (e.g. NEURON CUBA reference ~15,000 spikes/s biological time over 4000 cells -> ~few Hz/cell).",
        "Subthreshold voltage fluctuations: membrane potential mean sits several mV below threshold with standard deviation of a few mV (broad V distribution), consistent with fluctuation-driven (balanced) firing rather than mean-driven."
      ],
      "dewdrop_mapping": "Direct mapping onto Dewdrop primitives: (1) Neurons -> 4000 LIF structs with tau_m=20 ms (or g_L=10 nS, C_m=0.2 nF), V_rest=-60 mV (COBA)/-49 mV (CUBA), V_th=-50 mV, V_reset=-60 mV, t_ref=5 ms; the fixed-step exponential-Euler engine integrates the leak exactly (dt=0.1 ms recommended). (2) CUBA variant maps cleanly TODAY: CurrentSynapse (CUBA exponential) with tau_exc=5 ms, tau_inh=10 ms, and current quanta = g_quantum x (Vbar - E_rev) with Vbar=-60 mV -> EPSC +16.2 pA, IPSC -90 pA (using 0.27/4.5 nS); two synapse populations (E with +, I with -). (3) COBA variant requires a ConductanceSynapse (g(t) increments of 6 nS / 67 nS, decay 5/10 ms, reversals E_ex=0 mV, E_in=-80 mV, current = -g(t)(V-E_rev)); exponential-Euler handles the per-step conductance decay --- this is the synapse type to add for full VA parity. (4) Connectivity -> SparseCSR built by FixedProb(p=0.02) over 3200 E and 800 I source populations (Bernoulli per pair); store E and I weights as the synaptic values. (5) Transmission delay -> per-synapse DelayBuffer set to a uniform 0.1 ms (so >= 1 step at dt=0.1 ms). (6) Ignition -> counter-based Poisson drive delivering a brief (t in [0,50] ms) high-rate excitatory barrage to a random subset of neurons (e.g. NEST: 50 cells/pop at 300 Hz, or PyNN: ~20 sources at 100 Hz), then switched off so the recurrent network self-sustains; this exercises the (to-come) Poisson primitive. Validation: assert mean rate 2-15 Hz, mean CV_ISI 0.7-1.3, low pairwise correlation, and non-zero stationary activity at t=1 s. CUBA is the immediate target; COBA gates on adding ConductanceSynapse + FixedProb + Poisson primitives.",
      "sources": [
        "Vogels TP, Abbott LF (2005). Signal Propagation and Logic Gating in Networks of Integrate-and-Fire Neurons. J Neurosci 25(46):10786-10795. https://www.jneurosci.org/content/25/46/10786",
        "Brette R, et al. (2007). Simulation of networks of spiking neurons: a review of tools and strategies. J Comput Neurosci 23:349-398 --- Appendix 2 'Benchmark simulations' (definitive parameter transcription: 4000 IF cells, 80/20, 2% connectivity, g_L=5e-5 S/cm^2, area 2e4 um^2 -> tau_m=20 ms, E_L=-60 mV, V_th=-50 mV, V_reset=-60 mV, t_ref=5 ms; COBA quanta 6 nS/67 nS, tau 5/10 ms, E_rev 0/-80 mV; CUBA quanta 0.27 nS/4.5 nS, Vbar=-60 mV, E_L=-49 mV; 50 ms random ignition). arXiv:q-bio/0611089",
        "PyNN VAbenchmarks example (IF_cond_exp / IF_curr_exp; COBA Gexc=4 nS, Ginh=51 nS; CUBA Gexc=0.27 nS, Ginh=4.5 nS; n=4000, r_ei=4, pconn=0.02, delay=0.2 ms, tau_m=20, tau_syn_E=5, tau_syn_I=10, v_thresh=-50, v_reset=-60, tau_refrac=5). http://neuralensemble.org/docs/PyNN/0.8/examples/VAbenchmarks.html and https://neuralensemble.org/docs/PyNN/0.7/examples/VogelsAbbott.html",
        "NEST Benchmark 1 (COBA, iaf_cond_exp): NE=3200, NI=800, eps=0.02, delay=0.1 ms, E_L=-60, V_th=-50, V_reset=-60, t_ref=5, C_m=200 pF, g_L=10 nS, E_ex=0, E_in=-80, tau_syn_ex=5, tau_syn_in=10, w_exc=6 nS, w_inh=-67 nS, ignition Poisson 300 Hz, 1-51 ms, 50 cells/pop. https://nest-simulator.readthedocs.io/en/latest/auto_examples/brette_et_al_2007/coba.html",
        "NEST Benchmark 2 (CUBA, iaf_psc_exp): E_L=-49 mV, V_th=-50 (paper -50; NEST page lists threshold/reset details), tau_syn 5/10 ms, weights +16.2 pA / -139.5 pA, delay 0.1 ms, 10 s sim. https://nest-simulator.readthedocs.io/en/latest/auto_examples/brette_et_al_2007/cuba.html",
        "Brian2 frompapers CUBA/COBA (Stimberg et al. 2018 example_1_COBA): 4000 LIF, 3200 E/800 I, exponential synapses, dt=0.1 ms, 1 s, AI raster. https://brian2.readthedocs.io/en/stable/examples/frompapers.Stimberg_et_al_2018.example_1_COBA.html",
        "ModelDB 83319 (Brette et al. 2007 benchmark code, all simulators). https://modeldb.science/83319"
      ]
    }
  ],
  "spec": {
    "m2_features_needed": [
      "1. PER-NEURON INPUT VECTOR + synapse-into-engine wiring (FOUNDATIONAL, gates everything). Engine.jl currently drives ALL units with one constant scalar `input` and `asymptote(model, integ.input)` is scalar; `:deliver`/`:propagate` are no-ops; the integrator has no synaptic state. Add: (a) a per-unit input-current buffer `Iext::Vector{T}` (length N) in DewdropIntegrator so `:integrate` uses `@. V∞ = EL + R*Iext` (vectorised asymptote); (b) one or more synaptic-current accumulator columns (e.g. `Ie`, `Ii` for the two CUBA channels) carried in the integrator; (c) implement `:deliver` = `collect_due!` from the DelayBuffer added into the synaptic accumulators, `:integrate` decays each accumulator by `synapse_decay` and sums them into the total input current, `:propagate` = for each spiked presynaptic neuron walk `for_each_post` and `deposit!` weight*sign into the DelayBuffer at `now+delay`. This converts the existing CurrentSynapse + DelayBuffer + SparseCSR primitives (already in M1) into a working recurrent CUBA loop. REQUIRED FOR BOTH SCENARIOS.",
      "2. FixedProb / Bernoulli connectivity constructor. A generator `fixedprob_edges(rng_seed, npre, npost, p; weight, delay_steps, allow_autapses=false)` that emits the `(pre,post,weight,delay)` edge tuple list consumed by the existing `SparseCSR(arch, edges; npre, npost)` constructor. Must use the counter-based Philox `draw_uniform` (per ordered pair keyed by (pre,post)) so connectivity is seed-reproducible and order-independent (GPU-readiness contract). Vogels-Abbott uses Bernoulli p=0.02 per ordered pair; Brunel uses fixed-IN-DEGREE sampling (exactly C_E=1000 E-presynaptic + C_I=250 I-presynaptic chosen WITHOUT replacement per postsynaptic neuron) so also add a `fixed_indegree_edges(...; C, ...)` variant. REQUIRED FOR BOTH.",
      "3. Multi-population compose / two weighted projections into one engine. The engine must support an E population and an I population (or one partitioned population of N with an E-range and I-range) sharing the same LIF model, with TWO connectivity objects (E->all with +weight into the Ie accumulator, I->all with the I weight into the Ii accumulator, or a single signed-weight CSR over the concatenated 0:N source index space). Minimal viable form per the design memo: ONE concatenated SoA population of N=N_E+N_I LIF neurons + one SparseCSR over all N sources with signed weights, plus a per-edge channel tag OR two separate CSRs (E and I) so excitatory and inhibitory currents can target different synaptic time constants / reversal potentials. REQUIRED FOR BOTH.",
      "4. Counter-based Poisson spike-source drive. A `PoissonInput` operator: per neuron per step, draw Bernoulli(rate*dt) (or for many independent afferents, sample the summed Poisson/Binomial count) via `draw_uniform(T, seed, step, entity)`, and inject `count * J_ext` into the synaptic accumulator (collapsing C_ext independent afferents at rate nu_ext into one effective rate C_ext*nu_ext per neuron, as the research notes). Must support (a) a CONSTANT background rate (Brunel: rate = C_ext*nu_ext = 1000*eta*10 Hz on every neuron for the whole run) and (b) a TIME-WINDOWED transient (Vogels-Abbott ignition: high-rate barrage to a random neuron subset for t in [0,50] ms then off). REQUIRED: Brunel needs (a); Vogels-Abbott self-sustaining needs (b).",
      "5. ConductanceSynapse (COBA) synapse model. New `AbstractSynapseModel` carrying decay tau and a reversal potential `Erev`; state is a conductance accumulator `g` that increments by the quantal weight on a delivered spike, decays by `exp(-dt/tau)`, and contributes current `-g*(V - Erev)` to the neuron. Needs the `:integrate` phase to read V (voltage-coupled, unlike CUBA's voltage-independent current) so the synaptic-current sum becomes `-ge*(V-Eex) - gi*(V-Ein)`; this couples the synapse into the membrane equation and must remain compatible with the exact-propagator step (use the effective-conductance / exponential-Euler form: fold the total instantaneous conductance into an effective decay and asymptote each step). REQUIRED ONLY FOR Vogels-Abbott COBA (Benchmark 1); NOT needed for the CUBA-first targets. Gate this behind the CUBA milestones.",
      "6. Spike-time recording / raster + per-neuron ISI recording. Current `:record` only accumulates a scalar `spike_count`. To compute CV_ISI, population-rate power spectra, pairwise correlations and to draw rasters, add a recording mode that captures spike (neuron,step) events (or a per-neuron last-spike-step + ISI accumulator, or a downsampled bit-packed raster for a monitored subset). Needs a population-rate time series (spikes-per-bin) for the spectrum. REQUIRED FOR BOTH (all validation statistics + every canonical figure depend on it).",
      "7. Analysis utilities (test-side, can live in test/ or an ext): mean firing rate, per-neuron CV_ISI, population-rate PSD (FFT of binned population rate -> dominant peak frequency), and mean pairwise spike-count correlation over a monitored subset. These back the quantitative assertions. REQUIRED FOR BOTH.",
      "8. Heterogeneous / time-varying per-neuron input plumbing (minor): `DewdropNetwork.input` is a scalar today; generalise to accept a vector or an input operator so Poisson drive and recurrent synaptic current both feed the membrane. Folds into feature 1."
    ],
    "vogels_abbott_cuba_spec": "SCENARIO: Vogels-Abbott (2005) CUBA benchmark (Brette et al. 2007 App.2 Benchmark 2). FIRST M2 TARGET (maps onto the existing CurrentSynapse; needs only features 1-4,6,7 -- NOT COBA).\n\n(1) EXACT PARAMETER SET TO ENCODE (work in SI base units internally; volts shown in mV for clarity, convert to V or keep a consistent mV/ms/nF system -- recommend mV, ms, nF, nS, pA so dt=0.1, tau in ms):\n  Neuron (shared E+I), LIF(; tau=20.0 [ms], EL=-49.0 [mV] (CUBA depolarised rest), Vθ=-50.0 [mV], Vr=-60.0 [mV], R, tref=5.0 [ms]). Use C_m=0.2 nF, g_L=10 nS so tau_m=C_m/g_L=20 ms and R=1/g_L=100 MOhm=0.1 GOhm; with current in pA and R in (mV/pA)=GOhm, R=0.1 so R*I[pA] gives mV. NOTE: with EL=-49 and Vθ=-50, EL is ABOVE threshold -- the depolarised CUBA rest is what makes the network self-igniting/self-sustaining; this is intentional (Brette App.2).\n  Network: N=4000 = 3200 E (80%) + 800 I (20%). One concatenated LIF population of 4000; E-range 1:3200, I-range 3201:4000.\n  Connectivity: FixedProb / Bernoulli p=0.02 over every ordered pair (independent), giving mean in-degree ~80 (~64 E + ~16 I). Two projections: E-sources (1:3200)->all with the excitatory weight into Ie; I-sources (3201:4000)->all with inhibitory weight into Ii.\n  Synapses (CUBA, current-based exponential): tau_exc=5.0 ms, tau_inh=10.0 ms. Current quanta computed as g_quantum*(Vbar - Erev) with Vbar=-60 mV: use NEST/Brette CUBA numbers EPSC=+16.2 pA (=0.27 nS * 60 mV), IPSC=-90 pA (=4.5 nS * 20 mV). (PyNN variant uses the same 0.27/4.5 nS; NEST's iaf_psc_exp lists -139.5 pA after its kernel normalisation -- prefer the analytic +16.2/-90 pA so the EPSP/IPSP amplitudes match the paper.) So J_exc=+16.2 pA added to Ie on each E spike delivery, J_inh=-90 pA added to Ii on each I spike delivery.\n  Delay: single global synaptic delay D=0.1 ms = 1 step at dt=0.1 ms (DelayBuffer maxdelay=1). (PyNN uses 0.2 ms=2 steps -- acceptable.)\n  External drive: NO continuous Poisson. Ignition only: deliver a transient excitatory Poisson barrage to a random subset for t in [0,50] ms then switch off (NEST recipe: 50 cells per population at 300 Hz; PyNN recipe: ~20 sources at 100 Hz). After 50 ms the recurrent network self-sustains.\n  Engine: FixedStep dt=0.1 ms; integrate via exact propagator; threshold V>=Vθ, reset to Vr, hold tref=5 ms. Run >=1 s (test to 1-2 s; reference runs to 10 s). Discard first ~100-200 ms transient before computing statistics.\n  Initial V: randomise V in [Vr, Vθ] = [-60,-50] mV across neurons (uniform, via draw_uniform) so the network is not perfectly synchronous at t=0.\n\n(2) DEWDROP FEATURES THIS NEEDS: feature 1 (per-neuron input + CUBA synapse wired into engine, two accumulators Ie/Ii with tau 5/10 ms), feature 2 (FixedProb Bernoulli p=0.02 edge generator), feature 3 (two-projection / concatenated E+I population, signed weights), feature 4b (time-windowed transient Poisson ignition to a random subset), feature 6 (spike recording for raster/ISI/population rate), feature 7 (rate/CV/correlation/PSD analysis). Does NOT need COBA (feature 5).\n\n(3) CLASSICAL FIGURES (CairoMakie + Fathom; set_theme!(fathom()); sentence-case labels; addlabels!): \n  Fig VA-1 Spike raster: x=time (e.g. 200-1000 ms), y=neuron index for a random subset (~200-500 neurons or all E). CORRECT RESULT: sparse, irregular dots, NO vertical synchronous bands, activity persists across the whole window (self-sustained). \n  Fig VA-2 Population PSTH: network instantaneous rate (bin ~5-10 ms) vs time. CORRECT: roughly flat/stationary at a few Hz (~4-10 Hz) after the 50 ms ignition, only small (~1/sqrt(N)) fluctuations, no oscillatory peaks. \n  Fig VA-3 Single-neuron V(t): subthreshold trajectory of one neuron showing large noise-driven fluctuations and occasional threshold crossings (Brette Fig 12 left). Optionally a V-distribution histogram sitting several mV below threshold with a few-mV spread. \n  Fig VA-4 ISI histogram + per-neuron CV_ISI distribution: near-exponential ISI; CV_ISI distribution peaked near 1. \n  Use TwoPanel/FourPanel constructors; combine raster+PSTH as a stacked two-panel as the headline.\n\n(4) QUANTITATIVE TEST ASSERTIONS: see test_assertions array (VA entries).\n\nWHY FIRST: the CUBA current synapse already exists (CurrentSynapse exponential), the LIF propagator is exact and tested, DelayBuffer + SparseCSR exist; only the engine-wiring + FixedProb + Poisson-ignition + recording need building. No new synapse MODEL math (no voltage coupling) is required, so it is the smallest delta from M1 to a published benchmark.",
    "brunel_spec": "SCENARIO: Brunel (2000) sparse E/I LIF network. RICHER FOLLOW-UP (after Vogels-Abbott CUBA passes). Still CUBA (delta-current), so reuses the CUBA engine wiring; adds fixed-in-degree connectivity, continuous Poisson drive, longer-delay ring buffer, and a 4-regime sweep.\n\n(1) EXACT PARAMETER SET TO ENCODE (Brunel works in mV measured from rest, dimensionless; recommend mV + ms units, dt=0.1 ms):\n  Neuron (shared E+I), LIF(; tau=20.0 [ms], EL=0.0 [mV rest], Vθ=20.0 [mV] (=theta), Vr=10.0 [mV], R folded into the PSP jump, tref=2.0 [ms]). The PSP amplitude is carried by the synaptic WEIGHT (on-spike voltage increment), not by R*I; emulate the delta-current synapse either as (a) a true on-spike V-increment V += J after delay D, or (b) a CurrentSynapse with tau_syn << dt (delta limit). Prefer (a) for fidelity: weight J directly increments V of the post neuron when the delayed spike is delivered.\n  Network: N_E=10000 excitatory, N_I=2500 inhibitory (gamma=N_I/N_E=0.25), N=12500.\n  Connectivity: FIXED IN-DEGREE (sparse random, epsilon=0.1): every neuron receives EXACTLY C_E=epsilon*N_E=1000 recurrent excitatory + C_I=epsilon*N_I=250 recurrent inhibitory inputs (C=1250). Use the fixed_indegree_edges variant (sample C_E presynaptic E-neurons and C_I presynaptic I-neurons without replacement per postsynaptic target), NOT plain Bernoulli, to match Brunel's fixed-C convention.\n  Weights: excitatory J_E = J = 0.1 mV; inhibitory J_I = -g*J (g = inhibition/excitation ratio = primary control parameter). External weight = J_E = 0.1 mV.\n  Delay: uniform D=1.5 ms on EVERY connection = round(1.5/0.1)=15 steps; DelayBuffer maxdelay=15.\n  External drive: each neuron receives C_ext=C_E=1000 independent excitatory Poisson afferents at rate nu_ext, each weight J_E -- COLLAPSE to one effective Poisson input per neuron at rate C_ext*nu_ext = 1000*nu_ext, weight J_E (feature 4a, continuous background for whole run). nu_ext = eta * nu_thr, with nu_thr = theta/(J_E*C_E*tau_m) = 20/(0.1*1000*20) = 10 Hz EXACTLY (note tau in ms here: 20mV/(0.1mV*1000*20ms)=20/2000 per ms =0.01/ms=10 Hz). So nu_ext = eta*10 Hz; effective per-neuron external rate = 1000*eta*10 = 10000*eta Hz.\n  Engine: FixedStep dt=0.1 ms; exact LIF propagator; threshold V>=20mV, reset to 10mV, hold tref=2ms. Run 1-2 s, discard first ~200-500 ms transient.\n  FOUR REGIMES (each = a (g, eta) point; Brunel Fig 8): \n    SR (synchronous regular): g=3, eta=2. \n    AI (asynchronous irregular): g=5, eta=2. \n    SI-fast (synchronous irregular fast): g=6, eta=4. \n    SI-slow (synchronous irregular slow): g=4.5, eta=0.9. \n  (Brian2's panel labelling differs; use these (g,eta) pairs which match the research's regime->coordinate mapping.)\n\n(2) DEWDROP FEATURES THIS NEEDS: feature 1 (engine CUBA/delta wiring -- reuse from VA, plus a delta/on-spike-V-increment synapse OR tau_syn<<dt CurrentSynapse), feature 2 (fixed_indegree_edges generator), feature 3 (E+I populations / signed weights, two projections), feature 4a (CONTINUOUS background Poisson at rate C_ext*nu_ext per neuron, weight J_E), feature 6 (spike recording: raster + population-rate time series at fine bins for the PSD), feature 7 (rate, CV_ISI, population-rate PSD->peak frequency, pairwise correlation). Long delay (15 steps) exercises the DelayBuffer ring more than VA's 1 step. Does NOT need COBA.\n\n(3) CLASSICAL FIGURES (CairoMakie + Fathom): \n  Fig BR-1 Per-regime composite (Brunel Fig 8 a-d): a 4-row (or 2x2) panel, one column per regime {SR, AI, SI-fast, SI-slow}; each cell = (top) spike raster of ~50 neurons over a short window (e.g. 200 ms) + (bottom) population firing-rate histogram (instantaneous population rate, fine bin ~0.1-1 ms, lightly smoothed) over the same window. CORRECT RESULTS: SR = near-vertical regular bands (synchrony) + sharp periodic population peaks at the single-cell rate; AI = scattered irregular dots + near-flat population rate (small fluctuations, no oscillation); SI-fast = irregular sparse dots BUT fast (~150-200 Hz) population-rate oscillation; SI-slow = irregular sparse dots + slow (tens of Hz) population-rate oscillation. \n  Fig BR-2 Phase diagram in (g, eta) plane: g in [0,8], eta in [0,4]; partition into SR / AI / SI-fast / SI-slow regions with the mean-field linear-instability boundary lines overlaid; mark the four representative points. (Boundary lines can be the analytic Brunel curves; minimally, scatter the four simulated points coloured by detected regime.) \n  Fig BR-3 Population-rate power spectrum per regime: flat (AI), sharp peak ~150-200 Hz (SI-fast), low-frequency peak (SI-slow), peak at single-cell rate (SR). \n  Fig BR-4 ISI distribution + CV_ISI per regime (regular near-delta SR; exponential CV~1 AI/SI). \n  Fig BR-5 Single-neuron V(t) per regime (regular sawtooth SR; noisy fluctuating AI/SI). \n  Use FourPanel for the 2x2 regime composite and the per-regime spectra.\n\n(4) QUANTITATIVE TEST ASSERTIONS: see test_assertions array (Brunel entries).\n\nWHY SECOND: reuses the CUBA engine path validated by Vogels-Abbott, then adds the harder pieces (fixed-in-degree sampling, continuous Poisson, a 4-point regime sweep with spectral/correlation discrimination, long delay line). It is the definitive cross-simulator benchmark (NEST/Brian2/PyNN all ship it) so it is the strongest validation, but depends on more new machinery, hence the follow-up.",
    "figures_to_reproduce": [
      "VA-1: Vogels-Abbott CUBA spike raster (time vs neuron index, random subset ~200-500 neurons, 200-1000 ms) -- correct = sparse, irregular, asynchronous, no vertical synchrony bands, self-sustained across whole window.",
      "VA-2: Vogels-Abbott CUBA population PSTH (network instantaneous rate, ~5-10 ms bins) -- correct = roughly flat/stationary at a few Hz (~4-10), no oscillatory peaks.",
      "VA-3: Vogels-Abbott single-neuron V(t) subthreshold trace -- correct = large noise-driven fluctuations, occasional threshold crossings (fluctuation-driven), plus optional V-distribution histogram sitting several mV below threshold.",
      "VA-4: Vogels-Abbott ISI histogram + per-neuron CV_ISI distribution -- correct = near-exponential ISI, CV_ISI distribution peaked near 1.",
      "VA-headline: stacked TwoPanel of raster (top) + PSTH (bottom).",
      "BR-1: Brunel 4-regime composite (2x2 or 4-column), each cell = spike raster of ~50 neurons (top) + population-rate histogram (bottom) over a short window, for SR/AI/SI-fast/SI-slow -- correct per-regime morphology as described (regular synchronous bands for SR; irregular flat for AI; fast oscillation for SI-fast; slow oscillation for SI-slow).",
      "BR-2: Brunel phase diagram in (g,eta) plane with the four regions and the four marked representative points.",
      "BR-3: Brunel population-rate power spectrum per regime (flat AI; sharp 150-200 Hz peak SI-fast; low-freq peak SI-slow; firing-rate peak SR).",
      "BR-4: Brunel ISI distribution + CV_ISI per regime.",
      "BR-5: Brunel single-neuron V(t) per regime (regular sawtooth SR vs noisy AI/SI)."
    ],
    "test_assertions": [
      "VA-CUBA mean firing rate: 2 Hz < population mean rate < 15 Hz (commonly ~4-10 Hz), measured after discarding the first ~100-200 ms transient.",
      "VA-CUBA irregularity: 0.7 < mean(per-neuron CV_ISI) < 1.3 (Poisson-like, not regular/bursting); compute over neurons with >=3 spikes.",
      "VA-CUBA asynchrony: mean pairwise spike-count correlation |r| small (< 0.1, typically near 0) over a monitored subset; population-rate power spectrum flat (no dominant oscillatory peak).",
      "VA-CUBA self-sustaining: total spike count non-zero and activity stationary at t=1 s (still firing after ignition removed at t=50 ms); assert rate at [0.9s,1.0s] is within a factor ~2 of rate at [0.2s,0.3s] (no die-out, no runaway).",
      "VA-CUBA subthreshold: monitored neuron mean V sits several mV below Vθ=-50 with std of a few mV (fluctuation-driven balance).",
      "Brunel nu_thr == 10 Hz EXACTLY: assert theta/(J_E*C_E*tau_m) == 10.0 (20/(0.1*1000*20[ms]) in Hz). Unit test independent of simulation.",
      "Brunel AI (g=5,eta=2): 0.8 <= mean(CV_ISI) <= 1.1 AND population-rate spectrum flat (no peak significantly above the 1/sqrt(N) noise floor) AND mean pairwise spike-count correlation |r| <~ 0.01.",
      "Brunel SR (g=3,eta=2): mean(CV_ISI) < 0.3 (highly regular) AND a dominant population-rate spectral peak at approximately the mean single-cell firing rate AND clearly positive pairwise correlations (synchrony, r >> 0).",
      "Brunel SI-fast (g=6,eta=4): dominant population-rate spectral peak in ~[140,220] Hz (delay-controlled fast oscillation) AND single-cell CV_ISI ~0.8-1.0 (irregular) AND single-cell rate << the oscillation frequency (sparse firing).",
      "Brunel SI-slow (g=4.5,eta=0.9): dominant population-rate spectral peak at a LOW frequency (low tens of Hz) and clearly << the SI-fast peak AND irregular sparse single-cell firing (CV_ISI ~1).",
      "Brunel regime discrimination summary: SR/SI correlations clearly > 0 while AI correlation ~0; SI-fast peak > SI-slow peak; SR peak at firing rate not at the delay frequency.",
      "Cross-cutting (both): with a fixed RNG seed, connectivity generation and Poisson drive are reproducible (re-running yields identical spike counts), validating the counter-based RNG path."
    ],
    "sequencing": "Implement in this order. PHASE A (engine foundation, gates everything): build feature 1 -- per-neuron input vector + wire CurrentSynapse/DelayBuffer/SparseCSR into the engine's :deliver/:integrate/:propagate phases (today they are no-ops and input is a scalar). Validate with a 2-neuron deterministic recurrent test (one spike -> delayed PSC -> measured post-V bump) before any network. PHASE B (connectivity + drive primitives): feature 2 (FixedProb Bernoulli edge generator + fixed_indegree variant, both Philox-seeded), feature 3 (concatenated E+I population / two signed projections), feature 4 (counter-based Poisson: 4a continuous background, 4b time-windowed transient), feature 6 (spike recording: raster events + binned population-rate series), feature 7 (rate/CV_ISI/PSD/correlation analysis, test-side). PHASE C (FIRST BENCHMARK -- Vogels-Abbott CUBA): assemble the 4000-neuron CUBA network using only Phase A/B pieces (no COBA), run >=1 s, assert the five VA-CUBA statistics, and produce figures VA-1..VA-4 + headline. This is the smallest path to a published benchmark. PHASE D (SECOND BENCHMARK -- Brunel): add the delta/on-spike-V-increment synapse (or tau_syn<<dt), the fixed-in-degree connectivity, continuous Poisson at C_ext*nu_ext, and the 15-step delay; run the four (g,eta) regime points; assert the Brunel statistics (incl. the nu_thr==10 Hz unit test, which can land in Phase A immediately as it is simulation-independent); produce figures BR-1..BR-5. PHASE E (OPTIONAL / LATER -- Vogels-Abbott COBA Benchmark 1): only now add feature 5 (ConductanceSynapse with voltage coupling -ge*(V-Eex)-gi*(V-Ein), reversals Eex=0/Ein=-80 mV, quanta 6/67 nS, EL=-60 mV), since it requires new voltage-coupled propagator math; gate it strictly behind the CUBA benchmarks passing. RATIONALE: each phase reuses the previous; Vogels-Abbott CUBA is first because it needs zero new synapse math (CurrentSynapse already exists) and is the smallest delta from M1; Brunel is the richer cross-simulator gold standard but needs more new machinery (fixed-in-degree, continuous Poisson, regime sweep, spectral discrimination); COBA is deferred because it alone introduces voltage-coupled conductance dynamics."
  }
}

```
